The affordability tracker needs the following changes. I need them all confirmed that they executed correctly without breaking anything.

- Inside each of the cards, the circle with the first and last value are about half outside the window. Fix that the best way.
- For the state and compare state tab, remove the US average being there by default, and just have it as another state option.